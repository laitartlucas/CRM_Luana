export const CLIENT_SUCCESS_QUEUE = 'client-success';

// Dias após "Encerramento" (successStage=CLOSED) para o lembrete de
// renovação/indicação — mesmo idioma de FOLLOWUP_DAYS_AFTER em
// notifications/queue.constants.ts.
export const RENEWAL_REMINDER_DAYS_AFTER = 90;
