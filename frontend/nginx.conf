server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota que não seja um arquivo real cai no index.html
    # (React Router cuida do resto no client-side).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
